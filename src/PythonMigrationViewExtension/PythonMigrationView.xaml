<UserControl x:Class="Dynamo.PythonMigration.PythonMigrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dynamo.PythonMigration"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Name="MainGrid"
            Background="#353535">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="analysis"
                   Grid.Row="0"
                   Text="{Binding AnalysisResult}" FontSize="15"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right">
            <Button x:Name="convertButton" Content="Convert Code" Width="100"/>
            <Button x:Name="revertButton"
                    Content="Revert Code" Width="100"/>
        </StackPanel>
    </Grid>

</UserControl>
